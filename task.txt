我建议这样分：

1. `#2` 必修，而且应排第一。`OpenOT2/` 子模块现在是 “tracked 删除 + untracked 新包目录” 的混合状态；外层代码已经直接依赖这些新目录，比如 [plugin.py](/Users/tuomasier/Desktop/automation_lab/src/tasks/color_mixing/plugin.py:19)、[server.py](/Users/tuomasier/Desktop/automation_lab/scripts/server.py:20)、[pipeline.py](/Users/tuomasier/Desktop/automation_lab/src/pipeline.py:424)。如果子模块不先提交，外层 repo 的 commit 在 fresh clone/CI 上不成立。

2. `#1` 我也建议合并前修，但更准确地说它是“错误的死代码”。[scripts/server.py](/Users/tuomasier/Desktop/automation_lab/scripts/server.py:121) 把 `extra_routes()` 返回的 tuple 列表传给 `mount_app()`，而 [OpenOT2/openot2/webapp/app.py](/Users/tuomasier/Desktop/automation_lab/OpenOT2/openot2/webapp/app.py:189) 明确是收 FastAPI app，后面还会 `app.mount(path, sub)`。真 mount 一个 `list` 下去，请求时会报 `TypeError: 'list' object is not callable`。之所以当前测试没炸，是因为 extension 没有先 `bind()`，`extra_routes()` 通常直接返回 `None`，所以这段大多只是 no-op。最合理的修法就是删掉它，保留 [src/web/app.py](/Users/tuomasier/Desktop/automation_lab/src/web/app.py:121) 那条正确路径。

3. `#3` 建议在合并前修。[plugin.build_plan()](/Users/tuomasier/Desktop/automation_lab/src/tasks/color_mixing/plugin.py:181) 给出一个总次数，但 [loop._enrich_plan()](/Users/tuomasier/Desktop/automation_lab/src/web/loop.py:127) 在 pre-calibrated 时又悄悄改掉它。这样 plugin API 对直接调用者是不可信的。要么把 pre-calibrated 信息传进 plugin，要么让 loop 成为唯一的 total owner。

4. `#9` 很适合顺手修。[src/web/app.py](/Users/tuomasier/Desktop/automation_lab/src/web/app.py:128) 用 `hasattr(extra, "__iter__")` 太松，改成 `isinstance(extra, (list, tuple))` 就够了，1 行硬化。

5. `#5` 值得修，但属于 cleanup。[scripts/run_experiment.py](/Users/tuomasier/Desktop/automation_lab/scripts/run_experiment.py:52) 这里确实重复解析 config，`pipeline` 里又 load 一次。这不会立刻出错，但会继续放大 CLI / preflight / pipeline 的分叉。若要动，我会把 `#6` 一起收掉。

6. `#4` 我不建议现在做“大改成 runner step”。更实际的是把 [src/web/loop.py](/Users/tuomasier/Desktop/automation_lab/src/web/loop.py:201) 附近的表述改准：`$ref` 只覆盖 run 内 step wiring，`suggest_volumes` 仍然是 loop-level 的 pre-run 决策。除非你明确想把每次体积建议都变成可回放的 runner artifact，否则没必要为了叙事统一去重构。

7. `#7` 可暂缓，但如果你准备动 `#3` 的 state/plan ownership，可以一起收。[src/web/loop.py](/Users/tuomasier/Desktop/automation_lab/src/web/loop.py:267) 这段 snapshot/reset/restore 的确脆，不过当前行为有测试兜着，不像 blocker。

8. `#6` 单独看我会 defer。只是风格问题，收益不大；如果不连着 `#5` 一起做，性价比低。

9. `#8` 只是 hygiene。而且当前 `configs/calibrations/color_calibration/` 整个目录本身还是 untracked，先决定这些 calibration artifacts 到底要不要进 repo，再决定删空目录、加 `.gitkeep`，还是 ignore。

10. `#10` 我建议直接关掉，不用修。我核对了旧 loop diff 和新 plugin 实现，保存路径没变，都是 `.../calibrations/color_calibration/color_profile.json`。

我刚跑了 `pytest tests/test_web_protocol.py tests/test_pipeline_integration.py`，结果是 `89 passed`。所以这些问题大多是未覆盖路径和仓库状态问题，不是现有测试已经红掉。

如果你要一个最小、值得立刻做的修复集，我会排成：`#2 -> #1 -> #3 -> #9`。如果想顺手把 CLI 清干净，再加 `#5/#6`。